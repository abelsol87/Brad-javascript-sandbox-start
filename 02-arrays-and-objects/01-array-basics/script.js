let x;

// Array Literal
const numbers = [12, 45, 33, 29, 39, 12];
const mixed = [12, 'Hello', true, null];

// Array Constructor
const fruits = new Array('Apple', 'Grape', 'Orange');

x = numbers[0];

x = numbers[0] + numbers[5];

x = numbers.length;

fruits[1] = 'Pear';

x = fruits;

// fruits.length = 2;

fruits[fruits.length] = 'Blueberry';

console.log(x);

// Array Constructor